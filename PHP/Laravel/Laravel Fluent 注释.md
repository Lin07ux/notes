`src/Illuminate/Support/Fluent.php`


```php
/**
 * Class Fluent
 *
 * @package Illuminate\Support
 * @method $this after($column) 将该列置于另一个列之后(MySQL)
 * @method $this autoIncrement() 设置 INTEGER 列为自增主键
 * @method $this charset($charset) 指定数据列字符集(MySQL)
 * @method $this collation($collation) 指定数据列字符序(MySQL/SQL Server)
 * @method $this comment($comment) 添加注释信息
 * @method $this default($value) 指定列的默认值
 * @method $this first() 将该列置为表中第一个列 - MySQL
 * @method $this nullable($value = true) 允许该列的值为 NULL
 * @method $this storedAs($expression) 创建一个存储生成列(MySQL)
 * @method $this unsigned() 设置 INTEGER 列为 UNSIGNED(MySQL)
 * @method $this useCurrent() 设置 TIMESTAMP 列使用 CURRENT_TIMESTAMP 作为默认值
 * @method $this virtualAs($expression) 创建一个虚拟生成列(MySQL)
 */
```



